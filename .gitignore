node_modules/
# 빌드 산출물. 매 빌드마다 통째로 지우고 다시 만든다 — 원본은 bundle_config/deploy 다.
dist/
.agents/
.claude/settings.json
.env

# 설정 파일은 환경마다 다르고 자격증명이 들어간다. 샘플만 추적한다.
bundle_config/deploy/config/*.json
!bundle_config/deploy/config/project_sample.json
temp/
