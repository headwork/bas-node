{
    "_comment" : "프로젝트 설정 샘플. <프로젝트명>.json 으로 복사해서 쓴다. git 추적 대상은 이 파일뿐이다."
    , "_project" : "YAML 의 project 키가 어느 파일을 읽을지 정한다. project: \"hlngs\" -> hlngs.json"

    , "deploy" : {
        "_comment" : "state_path 는 파일·폴더 모두 허용한다. 폴더면 <yaml이름>.json 이 붙는다."
      , "state_path" : null
      , "state_keep" : 10
      , "lock_ttl_min" : 60
      , "jenkins_server_os" : "window"
      , "deploy_server_os" : "window"
    }

    , "jenkins" : {
        "_comment" : "토큰을 파일에 적지 않는다. .env 나 젠킨스 자격증명으로 넘기고 ${env.VAR} 로 참조한다."
      , "URL" : "https://localhost:8880/"
      , "USER" : "admin"
      , "API_TOKEN" : "${env.JENKINS_API_TOKEN}"
    }

    , "jenkins_dev" : {
        "_comment" : "개발서버 젠킨스"
      , "URL" : "https://<개발서버>:8880/"
      , "USER" : "admin"
      , "API_TOKEN" : "${env.JENKINS_DEV_API_TOKEN}"
    }
}
