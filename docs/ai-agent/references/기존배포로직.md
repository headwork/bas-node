# 1. 젠킨스 스캐줄
    git 변경내용 확인
    젠킨스 스케줄 - git diff
    변경내용이 있으면 plDeployWesysTest job 실행

# 2. plDeployWesysTest job
    hlngsGitPull job 실행

    hlngsBuildWesys job 실행
        
    hlngsUpload job 실행

    telegram job 실행

# 3. hlngsGitPull job
    git pull 처리

# 4. hlngsBuildWesys job
    "D:\Deploy\jenkins\project_hlngs\wesys_build.bat" 실행, 빌드명령
        %JENKINS_ROOT%\project_hlngs\wesys_build.bat %DEPLOY_GUBN%
        파일압축

# 5. hlngsUpload job
    scp 로 압축파일 업로드
    D:\HLNGS\wesys_restart.bat ${TAG_DEPLOY} 서버파일 실행
        로컬 파일 경로 "D:\Deploy\jenkins\project_hlngs\prod\wesys_restart.bat"
        백업, 압축해제, 서버정지, 배포파일 복사, 서버시작 ...

# 6. telegram job
    텔레그램 전송


# 빌드파일 압축 zip 파일로 압
tar -czf "%HLNGS_BUILD_HOME%\MFM.Shore.gz" -C "%HLNGS_BUILD_HOME%\MFM.Shore" .
변경 확장자 zip로
tar -a -cf "%HLNGS_BUILD_HOME%\MFM.Shore.zip" -C "%HLNGS_BUILD_HOME%\MFM.Shore" .


# 실제 운영환경
    개발서버
        젠킨스 설치됨
        git pull
        프로젝트 빌드
        운영서버 업로드

    운영서버
        배포파일 배포 및 재기동


# 운영서버 정보[QA]
    테스트
    배포파일 경로 : D:\HLNGS\build\MFM_QA.Shore.zip
    압축해제 경로 : "D:\HLNGS\build\MFM_QA.Shore"

    IIS 서비스명 MFM.SHORE_QA
    웹소스 배포 경로 : "D:\Deploy\MFM.SHORE_QA"
    백업파일 경로 : D:\HLNGS\backup

    SSH : 221.139.50.188 -p 9222

# 로컬서버 정보
    IIS 서비스명 MFM.SHORE_QA
    배포파일 경로 : "D:\Deploy\MFM.SHORE_QA"
    PORT(https) : 8443


tar -a -c -f "D:\Deploy\build\_artifacts\TEST_TOP.zip" -C "D:\Deploy\build\_publish" "MFM.SHORE_QA"

powershell -NoProfile -Command "Compress-Archive -Path 'D:\Deploy\build\_publish\MFM.SHORE_QA\*' -DestinationPath 'D:\Deploy\build\_artifacts\TEST_CA.zip' -Force"

icacls "D:\Deploy\build\_publish\MFM.SHORE_QA\Views\Crew\Certificate\CrewCertificateDetail_bak.cshtml"